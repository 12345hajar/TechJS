extends layout

block content
  h1(class="text-2xl font-bold mb-6") Connexion

  if registered
    .mb-4.rounded-md.bg-green-50.p-4.text-green-800
      | Votre compte a été créé avec succès. Vous pouvez vous connecter.

  if messages && messages.length
    .mb-4.rounded-md.bg-red-50.p-4.text-red-800
      ul.list-disc.pl-5
        each m in messages
          li= m

  form(action="/login" method="post" class="space-y-4 max-w-md")
    .flex.flex-col
      label(for="email" class="mb-1 text-sm text-gray-700") Email
      input(type="email" name="email" id="email" required class="border rounded-lg p-2")
    .flex.flex-col
      label(for="password" class="mb-1 text-sm text-gray-700") Mot de passe
      input(type="password" name="password" id="password" required class="border rounded-lg p-2")
    button(type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg") Se connecter
  p.mt-4.text-sm
    | Pas de compte ?
    a(href="/register" class="text-blue-600 underline") Inscrivez-vous
